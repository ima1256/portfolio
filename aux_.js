{
  loading && <Loading size={18} />;
}
{
  /* <MediaWithLoadEvent id="myvideoid">
        <video
          ref={videoRef}
          style={{
            width: '100%',
            height: '100%',
            objectFit: 'cover',
            opacity: loading ? 0 : 1,
            // display: loading ? 'none' : 'block',
            transition: 'opacity 0.5s ease-in-out',
          }}
          preload="auto"
          src={src}
          // poster={thumbnail}
          // muted={isMuted}
          autoPlay={true}
          loop
        />
      </MediaWithLoadEvent>


      <SliderLine videoRef={videoRef} /> */
}

{
  /* <AnimatedHeaderBrand
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1, y: 0, x: 0 }}
        transition={{ duration: 1 }}
        viewport={{ once: true, amount: 0 }}
      /> */
}

{
  /* <CustomVideo /> */
}
